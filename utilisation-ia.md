# Journal d'utilisation de l'IA — TourGuide

> Transparence OpenClassrooms : ce journal recense **chaque usage significatif** de
> l'IA sur le projet, au fil de l'eau. Il **prouve l'appropriation** du code — en mode
> compagnon, l'IA analyse et fait corriger, elle **n'écrit pas** le code métier ; les
> lignes ci-dessous en gardent la trace honnête.
>
> **Types d'interaction** — 🔍 Revue / analyse · 💡 Explication · ⚙️ Génération assistée · 🐞 Débogage.
> Les lignes sont **chronologiques** (on ajoute en bas, on ne réécrit pas l'historique).

| Étape / Tâche | Outil IA | Prompt (résumé) | Ce que l'IA a proposé | Décision | Vérification et conclusion |
|---------------|----------|-----------------|-----------------------|----------|----------------------------|
| 🔍 Mise en place du suivi qualité | Claude Code | « passe en mode compagnon, crée les fichiers de suivi IA et le résumé des skills personnels » | Création de `utilisation-ia.md` (ce journal), `rapport.md` (support de revue vierge) et `skills-perso.md` (résumé des skills perso) ; aucun code du projet touché | Gardé | Fichiers relus ; ils vivent à la racine, hors `src/`. Prochaine étape : lancer la revue pédagogique des 5 phases |
| 🔍 Revue pédagogique globale (5 phases) | Claude Code | « analyse mon projet » | Revue classée en 5 phases → `rapport.md` : outillage `pom.xml` absent (Phase 1), 2 bugs réels (`addUserReward`, `ConcurrentModificationException`), finition Phase 5 (Javadoc/README/config), tests à durcir. Aucun code du projet modifié — l'IA a seulement pointé et expliqué | Gardé | Rapport lu et compris ; je corrige moi-même via la checklist. Constat cohérent avec le starter OC. À vérifier ensuite en lançant les outils une fois le `pom.xml` outillé |
| ⚙️ Réparation du Maven wrapper | Claude Code | « j'ai modifié le README pour utiliser `./mvnw`, corrige le wrapper » | Diagnostic : `.mvn/wrapper/maven-wrapper.properties` manquant → `./mvnw` inutilisable. Régénération via `mvn -N wrapper:wrapper -Dmaven=3.9.9` : création de `.mvn/wrapper/maven-wrapper.properties` + mise à jour de `mvnw`/`mvnw.cmd` | Gardé | Vérifié par `./mvnw -v` → Maven 3.9.9 OK (exit 0). Version 3.9.x cohérente avec Spring Boot 3.1. `.mvn/` bien suivi par git |
| 🔍 Revue de mes corrections (CME + addUserReward) | Claude Code | « j'ai modifié mon code, `nearAllAttractions` passe, analyse mes modifs » | Revue du diff : fix `addUserReward` (comparaison String↔String via `anyMatch`) validé ; `CopyOnWriteArrayList` sur `visitedLocations`/`userRewards` résout la CME (cause réelle = itération concurrente avec le Tracker). Alerte perf sur COW pour `userRewards`. Aucun code écrit par l'IA | Gardé | Corrections faites par moi ; l'IA a confirmé la justesse et pointé le compromis perf de COW à réévaluer à l'étape performance |
| 🔍 Revue des ajustements IDE (nettoyage code propre) | Claude Code | « j'ai appliqué des recommandations de mon IDE, vérifie » | Revue du diff + `test-compile` (OK) : bons choix validés (`noneMatch`/`anyMatch`, `isEmpty()`, method refs, `assertEquals`/`assertFalse`, logs `{}` du Tracker, `final`, `cumulativeRewardPoints`). Alerte : indentation mixte tabs/espaces **aggravée** par le reformatage IDE. Restes : 1 log concaténé + `System.out` de test. Aucun code écrit par l'IA | Gardé | Compilation vérifiée verte ; je corrige l'indentation (config IDE uniforme + reformater les fichiers) et je finis logs/asserts |
| ⚙️ Création du `.editorconfig` | Claude Code | « crée le `.editorconfig` » | Fichier `.editorconfig` racine : Java = 4 espaces sans tab, ligne ≤ 120, YAML 2 espaces, UTF-8/LF, newline finale. Outillage (pas de code métier) | Gardé | Fichier posé ; reste de mon côté à décocher « Detect and use existing file indents » dans IntelliJ puis relancer Reformat sur `src` pour uniformiser en 4 espaces |
| ⚙️ Conversion indentation tab → 4 espaces sur `src` | Claude Code | « convertis / vérifie l'indentation » (IntelliJ ne convertissait pas malgré les réglages) | Conversion mécanique (`expand -i -t 4`) de toutes les tabulations d'indentation en 4 espaces sur les fichiers `.java`. Aucune logique modifiée | Gardé | Vérifié : 0 tab, 0 mélange, `test-compile` vert. Config IntelliJ désormais en espaces → ne se re-cassera pas. Effet révélé : 2 lignes > 120 dans `RewardsService` à couper |
| 🔍 Revue de `getTripDeals` (test réactivé + retour de 10 offres) | Claude Code | « oubli d'un `@Test`, la méthode doit retourner 10 objets, vérifie » | Test `getTripDeals` lancé → passe. Points soulevés : régression d'indentation (23 tabs réintroduits par l'IDE), `user.setTripDeals(...)` supprimé (setter devenu mort), approche « boucler sur TripPricer jusqu'à 10 » à interroger (déterminisme, `Set`/equals, perf, intention vs spec). Aucun code écrit par l'IA | Gardé | Test vérifié passant ; je décide de `setTripDeals`, je re-règle l'IDE, et je questionne le « 10 » vs la consigne |
| 🔍 Revue des finitions Phase 5 (logs / System.out) | Claude Code | « j'ai fait les finitions, vérifie » | Tests verts (12/0). Finitions validées (logs `{}`, `System.out` retirés, constante `NUMBER_OF_USERS`, method ref). Points relevés : log de `TestPerformance` mi-concaténé, ordre `private static final`, import `Tracker` inutilisé ; `setTripDeals` et les 2 lignes > 120 pas encore faits. Aucun code écrit par l'IA | Gardé | Je tape moi-même `setTripDeals` + coupe des lignes (mode compagnon : je garde la main sur le code) ; finitions relues |
| 💡🔍 Cadrage + revue du TODO getNearbyAttractions (DTO) | Claude Code | « cadre le TODO » puis « regarde mes modifications » | Cadrage : DTO aux frontières, tri + limit(5), exposer les points, démarche TDD. Revue du code produit **par moi** : DTO record + constructeur secondaire, injection constructeur du contrôleur, `@NotBlank`, `sorted().limit(5)` optimisé — validés (test vert). Points : indentation 2 espaces du DTO, `getRewardPoints` package-private, test à enrichir (points/distance). Aucun code écrit par l'IA | Gardé | Fonctionnalité codée par moi ; l'IA a cadré et relu. Reste : vérifier les points dans le test + reformater le DTO |
| 💡🔍 Performance : cadrage + revue de la parallélisation | Claude Code | « mesure d'abord » puis « les 2 tests passent, vérifie » | Cadrage : mesure de départ (7 s/100 users), goulot = latence `gpsUtil` en série, pistes `ExecutorService`/`CompletableFuture`. Revue de mon code parallèle (100 000 users : track 200 s, rewards 255 s, sous cibles) : pool fixe + `allOf().join()` + arrêt propre validés. Points : Tracker resté séquentiel, duplication executor, erreur = échec batch, Javadoc manquante. Non-régression 10/0 vérifiée par l'IA ; runs 100 000 non reproduits (sortie fournie par moi). Aucun code écrit par l'IA | Gardé | Parallélisation codée par moi ; l'IA a cadré, relu et validé la non-régression. Reste : Tracker, factorisation, Javadoc |
| 🔍 Revue branchement Tracker + factorisation `ParallelTaskExecutor` | Claude Code | « je branche le tracker, vérifie » | Revue de mon code : Tracker branché sur la version parallèle ; nouvelle classe utilitaire générique `ParallelTaskExecutor` (`<T>`+`Consumer`, `final`, Javadoc FR) factorisant le motif, avec test (nominal 1 000 + liste vide, structure thread-safe) ; 2 services réduits à une ligne. Non-régression 12/0 vérifiée par l'IA. Reste : Javadoc des 2 méthodes de service. Aucun code écrit par l'IA | Gardé | Refactorisation + tests écrits par moi ; l'IA a relu et validé. Volume remis à 100 pour le commit |
